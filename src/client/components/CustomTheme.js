import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: { main: '#000050' },
    secondary: { main: '#228B22' },
    light: { main: '#F5F5DC' },
    dark: { main: '#0A0E1B' },
  }
});
